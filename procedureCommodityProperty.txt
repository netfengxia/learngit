1.只是为了处理关于 新增商品时的情况.
主要思路就是用HashMap来处理键值对应关系.

前台传来的数据,是一个JSONObject.
它里面应该可以包含  属性名 属性值两个列表哦.
用来可以来生成相应数据及Map

对它们的处理主要是集中如下:(准备两个Map, PropertyMap, ValueMap)
	Map的存储格式, 是以 中文值为Key, 记录ID值为Value
	1.对于所有值(包含属性名, 属性值)进行属性存在与否,在当前商家下.
		1-1.不存在,就创建并且存入到相应的Map中
		1-2.若存在,就直接取出放到Map中
	
	准备好这两个Map以供下面调用.

对于每条存储记录,依然是应用JSONObject对象.对于异常, 处理是忽略.
应该先建立一个am_tuangou_sku表的记录,写入相应的价格和数量
再依据该sku_id再去am_tuangou_property中插入相关的属性名和属性值对就的ID,再与该sku_id相关.

